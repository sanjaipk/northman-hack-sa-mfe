import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'northman-mybh-sa-mfe4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
